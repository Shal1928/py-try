from core import *
from app import *


# main = cfg_map['common-core']
# main.append(create_backup(ip, username, password, main[1], server_backup_path, is_clear=True))
# revert_from_backup(ip, username, password, main[2], main[1])

# transfer_to_server(ip, username, password, main[0], excluded, main[1])
